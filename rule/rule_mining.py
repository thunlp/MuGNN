
def mine_single_language_rule(triples):
    '''
    rule patterns
    1. $$(x, r_s, y) \rightarrow (x, r_t, y)$$
    2. $$(x, r_{s_1}, y) \wedge (y, r_{s_2}, z) \rightarrow (x, r_t, z)$$
    
    return 
    '''
    def pattern1():
        pass

def main():
    pass